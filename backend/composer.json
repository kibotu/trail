{
    "name": "kibotu/trail-backend",
    "description": "Trail - Link Journal Backend API",
    "type": "project",
    "require": {
        "php": ">=8.4",
        "embed/embed": "^4.4",
        "firebase/php-jwt": "^6.10",
        "slim/psr7": "^1.7",
        "slim/slim": "^4.14",
        "symfony/yaml": "^7.2"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^11.5"
    },
    "autoload": {
        "psr-4": {
            "Trail\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Trail\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=512M",
        "lint": "php -l src/ && phpstan analyse --memory-limit=512M"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true,
        "classmap-authoritative": true,
        "apcu-autoloader": false,
        "discard-changes": true,
        "preferred-install": {
            "*": "dist"
        }
    }
}
